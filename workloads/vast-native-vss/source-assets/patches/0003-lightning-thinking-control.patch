diff --git a/source-code/retrieval/video-backend/src/config.py b/source-code/retrieval/video-backend/src/config.py
--- a/source-code/retrieval/video-backend/src/config.py
+++ b/source-code/retrieval/video-backend/src/config.py
@@ -63,6 +63,10 @@ class Settings(BaseSettings):
     llm_max_tokens: int = Field(default=1200, description="Max tokens per synthesis completion chunk")
     llm_max_continuations: int = Field(default=4, description="Extra continuation chunks when response hits token limit")
     llm_local_nim: bool = Field(default=False, description="True = use local NIM (llm_host/port), False = NVIDIA Cloud")
+    llm_enable_thinking: Optional[bool] = Field(
+        default=None,
+        description="When set, pass chat_template_kwargs.enable_thinking to the LLM API",
+    )
     
     # VAST VMS & Tenant (used for login; not sent from frontend)
     vast_host: str = Field(..., description="VAST management server address (VMS) for user authentication")
diff --git a/source-code/retrieval/video-backend/src/services/llm_service.py b/source-code/retrieval/video-backend/src/services/llm_service.py
--- a/source-code/retrieval/video-backend/src/services/llm_service.py
+++ b/source-code/retrieval/video-backend/src/services/llm_service.py
@@ -40,6 +40,7 @@ class LLMService:
         self.timeout = self.settings.llm_timeout_seconds
         self.max_tokens = self.settings.llm_max_tokens
         self.max_continuations = self.settings.llm_max_continuations
+        self.enable_thinking = self.settings.llm_enable_thinking
         # Default prompt is only used as fallback if frontend doesn't send one
         self.default_prompt = DEFAULT_SYSTEM_PROMPT
         print(
@@ -214,6 +215,10 @@ class LLMService:
                     "max_tokens": self.max_tokens,
                     "stream": False
                 }
+                if self.enable_thinking is not None:
+                    payload["chat_template_kwargs"] = {
+                        "enable_thinking": self.enable_thinking
+                    }
                 response = client.post(url, json=payload, headers=headers)
                 response.raise_for_status()
 
